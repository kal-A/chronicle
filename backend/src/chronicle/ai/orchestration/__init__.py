"""Bounded investigation execution and finalization modules.

Imports remain explicit at their concrete modules to avoid a package-level
cycle through model providers -> orchestration policy -> finalization -> agents.
"""
