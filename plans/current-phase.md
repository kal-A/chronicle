# Current Phase

**Phase:** AI-first pivot — Phase B complete; Phase C0/C1/C2/C3 complete; Phase D0 (map-first workspace, sub-plans D0.1-D0.6) complete, pending the actual human usability study before being treated as validated; Phase E0 complete (reorientation to the LLM-agent-system-is-product-core correction); **Phase E1 complete** (model-provider foundation — `ModelProvider` protocol, deterministic + Ollama providers) — Phase E2 (corpus service + agent tools) is next  
**Branch:** `phase-e-ai-core`, created from `phase-c-python-foundation` at commit `43d2b94` (Phase C0-C3, D0.1-D0.6, and E0 all committed and pushed to `origin/phase-c-python-foundation` before this branch was cut). `phase-e-ai-core` itself is not yet pushed — E1 is complete but uncommitted, awaiting separate explicit approval before commit/push, same standing rule as every prior phase.  
**Status:** The versioned package contract, golden JSON, generic renderer route, and preserved Explore facets are implemented and verified. `chronicle generate <any topic>` runs 9 deterministic mock providers through the resumable pipeline and produces an actual `GeneratedInvestigation` package that passes the same validator the frontend uses — for arbitrary topics, with clearly-synthetic, disclosed-as-mock content. A second, real curated provider set (`--provider-set concert-of-europe`) now proves the same pipeline carries genuinely researched content: a second, independently sourced package is registered in the frontend and rendered by the same generic route/components as the hand-authored blank-cheque package. No live provider, database, retrieval, embeddings, or model calls exist yet — Phase E1 is where that changes.

Kamal supplied a map-first workspace product correction (`docs/product/map-first-workspace-instructions.md`), turning the article-first renderer into a map-first workspace with a persistent docked/sheet assistant panel, historical lenses, and an Inspector mode. This was **Phase D** (`docs/decisions/ADR-002-map-first-workspace.md`), inserted ahead of the former Phase D ("Real Discovery and Assessment"). Phase D's six sub-plans (D0.1-D0.6) are all built and verified: the workspace/Inspector shell, the `InvestigationExperiencePlan` contract (TS/Python parity) generated by the real curated pipeline for Concert of Europe, the Ask entry-surface prototype, and the usability-gate test plan. **Committed and pushed** as `b0380e2` on `origin/phase-c-python-foundation`.

Kamal then supplied a second, larger product correction: the LLM/multi-agent system is the actual product core, not the historical content around it (`docs/decisions/ADR-003-llm-agent-system-is-product-core.md`, source instructions preserved at `docs/ai-core-instructions/`). This **replaces** the former Phase E ("Real Discovery and Assessment") with a new Phase E ("Real LLM Agent Core") — the whole former E-L sequence is replaced with a new E-K sequence (former Phase E/F content absorbed into new F/G; see `docs/delivery/revised-development-phases.md`). Phase E0 (this reorientation) is complete: a checkpoint commit of the entire pending C0-C3/D0.1-D0.6 body of work, the instruction set added to the repo, the ADR, the roadmap replacement, and additive notes on the docs already partially describing this direction. **Committed and pushed** as `43d2b94` on `origin/phase-c-python-foundation`. All of Phase C, D0, and E0 are now safely on the remote before Phase E1 work begins on the new `phase-e-ai-core` branch.

## Phase B Delivered

- Added the `GeneratedInvestigation` Zod contract, supported schema version, generation report, interaction specification, historical map asset/map scene, claim ledger, normalized evidence-link records, and cross-reference validation.
- Enforced unsupported-version, reference, synthesis-ledger, published-evidence, relationship-evidence, verification, rights/period-fit, and location-precision failures.
- Migrated the full prototype content into `fixtures/blank-cheque.golden-investigation.json` without changing its historical assertions.
- Removed the legacy TypeScript scene and static Scene provider.
- Added a validated package repository with success, honest partial/empty, not-found, scene-not-found, and simulated-failure modes.
- Added `normalizeInvestigationScene`, keeping the existing narrative/timeline/map/graph/evidence components while making the package canonical.
- Replaced the hard-coded scene/page with `/investigations/:packageId/scenes/:sceneId`; `/` redirects using registered default-package metadata.
- Added first-class claim, relationship, source, and passage focus alongside scene, event, entity, and time-range focus.
- Preserved keyboard, accessibility, URL focus, cross-facet, map, evidence, loading, empty, and failure foundations.

## Evidence and Source-Enrichment Invariant

New historical sources can be added to any period or event through package-level Source, Document, Passage, and EvidenceLink records. They influence conclusions or prose only when a supported Claim/Relationship/KnownAtTime is created or revised and a material NarrativeBlock explicitly references that record. A Source record by itself never mutates narrative text. Phase K will route these effects through immutable impact review and human acceptance before publication.

## Verification Recorded 2026-08-03

- `npm run lint` — passed.
- `npm run typecheck` — passed.
- `npm test -- --run` — 10 files, 72 tests passed; jsdom emits the known canvas-not-implemented diagnostic while accessible fallbacks are exercised.
- `npm run build` — passed; Vite reports the existing large MapLibre chunk advisory.
- `npm run test:e2e` — 5 Playwright browser journeys passed.
- Hard-coding acceptance search found no July Crisis/Blank Cheque/package fixture constants in non-test generic renderer, provider, or route files.
- Manual `agent-browser` walkthrough — passed root redirect, package-driven title, timeline/narrative/evidence sync, map place focus, graph claim and relationship focus, source and passage focus, browser-back restoration, and visible limitation/counterevidence checks.
- Browser QA found a stale event-specific HTML title; a failing regression assertion was added, the title became package-driven, and both the targeted test and live-browser reload passed.

## Phase B Closure

- The manual browser flow was walked on 2026-08-03 with `agent-browser` 0.33.2 and its managed Chrome runtime.
- The Phase B Codex feature-diff/historical-integrity handoff is available in `docs/delivery/phase-b-implementation-report.md`.
- No commit or push was performed.

## Completed: Phase C0 — Python Foundation and Contract Parity

Kamal supplied an adjusted Phase C plan (`chronicle_phase_c_adjusted_plan.md`) covering the full deterministic-generation phase plus a roadmap through Phase I. Per `CLAUDE.md`'s smallest-complete-vertical-slice rule, Phase C was broken into four sub-plans (C0–C3, mirroring how Phase 1's Plans 0–5 were sequenced and approved one at a time). **Only C0 is built; C1–C3 are sequenced but not started.**

**Assumptions stated and not yet separately confirmed** (per `CLAUDE.md` rule 4 — flagged here, not silently decided): Python tooling is plain `venv` + `pip` + `pyproject.toml` (setuptools) + `pytest`, no `poetry`/`uv` — this was an explicitly open decision in the adjusted plan's own text.

**Built, on a new `phase-c-python-foundation` branch (not merged):**
- `backend/` — a Pydantic v2 package (`chronicle.contracts`) mirroring `src/features/investigation/model/generatedInvestigation.ts` and its `schema.ts` dependencies field-for-field: every enum, every nested type, the exact same optional/required/min-length constraints, and the same two `HistoricalDate`/`NarrativeBlock` cross-field refinements Zod enforces.
- `backend/src/chronicle/contracts/validation.py` — a line-for-line port of `validateGeneratedInvestigation()`'s 20 imperative cross-record rules (ID uniqueness, every reference chain, evidence-role requirements, disputed-relationship counterevidence, map-marker precision vs. Place evidence, published-package gates), numbered and ordered to match the TS source so the two can be diffed by eye.
- `fixtures/contracts/invalid/*.json` — 5 new shared fixtures, each a minimal mutation of the existing golden package targeting one structurally distinct validator rule (unsupported version, dangling evidence reference, claim missing supporting evidence, disputed relationship missing counterevidence, map marker precision exceeding its Place's evidence).
- Cross-language parity proven both directions: Python's `pytest` suite loads `fixtures/blank-cheque.golden-investigation.json` directly (no duplication) and accepts it, and rejects all 5 invalid fixtures for the expected reason; a new TS test block in `generatedInvestigation.test.ts` loads the same 5 invalid fixtures and asserts `validateGeneratedInvestigation()` rejects them too.

**Verification:** Python — 8/8 pytest passed. Frontend — `npm run typecheck`, `npm run lint`, `npx vitest run` (77/77, up from 72), `npm run build`, `npm run test:e2e` (5/5) all pass clean, no regressions.

**Explicitly not in C0:** no workflow engine, no stage-run persistence, no CLI, no providers, no second package, no live calls.

## Completed: Phase C1 — Workflow Engine, File-Based Run Storage, CLI Skeleton

Still on `phase-c-python-foundation`, not merged. Proves the deterministic, resumable workflow engine's *mechanics* before any real (even mocked) domain-provider logic exists (C2).

**Stage taxonomy** (from the adjusted plan §4, used verbatim, not "mock"-prefixed so it stays valid once C2+ replace the stubs): `SCOPE_PROPOSED → DISCOVERY_QUERIES_PREPARED → SOURCE_CANDIDATES_DISCOVERED → SOURCES_ASSESSED → CORPUS_PREPARED → HISTORICAL_MODEL_ASSEMBLED → INVESTIGATION_COMPOSED → VERIFIED`, ending in one of `ready | partial | abstained | failed`.

**Built:**
- `backend/src/chronicle/workflow/` — `RunRecord`/`StageRecord` Pydantic models (fields per the adjusted plan §7), and `engine.py`'s `generate()`/`resume()`/`run_pipeline()`/`run_stage()`. Reuse-vs-rerun is decided per stage by comparing the current input's hash against the hash on that stage's last `PASSED` attempt — since each stage's input is exactly the previous stage's output, a changed upstream result automatically produces a different hash at the next stage, so downstream invalidation needs no separate bookkeeping pass.
- `backend/src/chronicle/storage/run_store.py` — file-based persistence (`backend/runs/<run-id>/run.json`, `stages/NN-<stage>.json`, `output/<stage>.json`), gitignored.
- `backend/src/chronicle/cli/` — `chronicle generate <topic> [--fail-at STAGE]`, `resume <run-id>`, `inspect <run-id>`, `validate <package-path>` (the last is a thin wrapper around C0's `validate_generated_investigation()`, independent of the workflow system). Registered as a console script.
- C1's 8 stage functions are deliberate, clearly-commented deterministic stubs (not real historical content) — their only job is proving the engine works. `--fail-at <stage>` is an explicit, documented flag (not a hidden magic string) for exercising the `FAILED`/retry/resume-after-failure path honestly.
- **A real circular import was hit and fixed during implementation**, not shipped: `workflow/__init__.py` eagerly re-exporting from `engine.py` (which imports `RunStore` from `storage`, which imports from `workflow.stages`/`state`) created a cycle depending on import order. Fixed by having `workflow/__init__.py` only re-export the cycle-free `stages`/`state` modules; consumers import `chronicle.workflow.engine` directly.

**Verification:** 30/30 pytest passed (8 contract + 6 storage + 9 CLI + 7 workflow-engine), including: fresh run completes all 8 stages `READY`; unchanged resume reuses every stage (attempt counts stay at 1, confirmed by inspecting persisted records, not just final status); a changed topic invalidates and reruns every stage; `--fail-at` produces `FAILED` with correct partial completion and attempt count; resuming a failed run without the induced fault succeeds and doesn't re-execute stages that already passed; two runs with identical input produce identical stage output hashes. Manually smoke-tested the actual CLI end-to-end (not just the test suite) for all four commands, including `validate` against both the real golden fixture and a C0 invalid fixture. Frontend — typecheck/lint/`npx vitest run` (77/77, unchanged) all pass; C1 touches no frontend files.

**Explicitly not in C1:** no real historical content, no domain provider logic, no second package, no live calls, no `PARTIAL`/`ABSTAINED` semantics (those need real corpus/evidence-quality judgment that only exists once C2 lands — deliberate deferral, not an oversight).

## Completed: Phase C2 — Deterministic Mock Providers, Assembler, Verifier

Still on `phase-c-python-foundation`, not merged. `chronicle generate <topic>` now produces a real, schema-valid `GeneratedInvestigation` package for *any* topic string. Per Kamal's explicit choice, content is **generic and topic-agnostic** — clearly-synthetic, deterministically-templated, disclosed as mock — proving the pipeline mechanically assembles a valid package end to end. Real historical curation for "The Concert of Europe and Revolutionary Intervention" is C3's job, researched the way Scene 2 actually was, not templated.

**Built, replacing C1's stubs without any change to `engine.py`'s stage-execution contract:**
- `backend/src/chronicle/providers/mock/` — the 9 providers from the adjusted plan §8 (scope, discovery-query, source-candidate, source-assessment, a deterministic — not "mock" — corpus assembler, historical-extraction, timeline, relationship, geography), composed in `providers/registry.py`'s `MOCK_STAGE_FNS` onto C1's existing 8 `StageName`s (no `STAGE_ORDER` change; extraction+timeline+relationship+geography all compose into `HISTORICAL_MODEL_ASSEMBLED`).
- `backend/src/chronicle/providers/verification.py` — the `VERIFIED` stage: runs the composed draft through C0's `validate_generated_investigation()` (deterministic software, never a "provider", per `AGENTS.md` §4) and returns the validated, re-serialized package as the pipeline's final output.
- **Genuine, non-contrived `PARTIAL` coverage:** the geography provider honestly has no real geography for a synthetic topic, so it produces no map asset — package `status` and `generationReport.outcome` both come out `"partial"`, with the omission disclosed in both places. Real coverage of validator rule 19 and the `PARTIAL` terminal state, not a contrived flag. `ABSTAINED` stays deferred.
- Determinism: every provider is a pure function; the composed package uses a fixed sentinel timestamp (not wall-clock) so two `generate()` calls with the same topic produce byte-identical packages.
- **A real bug was caught during manual smoke-testing, not shipped**: `run_stage()`'s reuse check compares `providerVersion`, but it was hardcoded regardless of which `stage_fns` were actually passed in — meaning resuming a run with a *different* provider set (e.g. C1 stubs → C2 real providers) would have wrongly reused stale output instead of re-running. Fixed by threading `provider_set_version` explicitly through `generate()`/`resume()`/`run_pipeline()`/`run_stage()`, tied to `run.providerSetVersion`; added a regression test.
- `stage_fns` is now a required (not defaulted) argument on `generate()`/`resume()`/`run_pipeline()` — the CLI passes `MOCK_STAGE_FNS`, engine-mechanics tests explicitly pass C1's `DEFAULT_STAGE_FNS` to keep mechanics isolated from content. `RunRecord.outputPackagePath`/`generationReportPath` (defined in C1, unused until now) are populated; the package and report are persisted to `output/package.json` / `output/generation-report.json`.

**Verification:** 49/49 pytest passed (8 contract + 6 storage + 9 CLI + 8 workflow-engine, incl. the new reuse regression test + 18 new provider tests). Manually smoke-tested the full CLI end-to-end, including feeding the CLI's own `output/package.json` back through `chronicle validate` — confirmed valid. Frontend — typecheck/lint/`npx vitest run` (77/77, unchanged) all pass; C2 touches no frontend files.

**Explicitly not in C2:** no real historical content (that's C3), no `ABSTAINED` scenario, no live calls, no frontend changes.

**No commit, no push, no merge** on C0, C1, or C2 — per `AGENTS.md` §9 and the adjusted plan's own instruction, that needs separate explicit approval.

## Completed: Phase C3 — Concert of Europe: Real Curated Package, Frontend Integration, Genericity Proof

Still on `phase-c-python-foundation`, not merged. A second, real, hand-researched investigation — "The Concert of Europe and Revolutionary Intervention, 1814-1822" (the Congress of Vienna, the Troppau Protocol, the Congress of Laibach and the Austrian intervention in Naples, and the Congress of Verona's authorization of French intervention in Spain) — generated end-to-end via `chronicle generate --provider-set concert-of-europe`, registered in the frontend fixture registry alongside the hand-authored blank-cheque package, and proven to render through the exact same generic route/components with a new Playwright journey.

**Assumption stated (per `CLAUDE.md` rule 4):** since no live retrieval/LLM layer exists yet (by design, deferred past Phase C), "real content generated via `chronicle generate`" means a second, topic-specific **curated** provider set — hand-researched and hand-written, exactly as rigorous as Scene 2's authoring, expressed as deterministic Python functions plugged into the same 8-stage engine contract, rather than a generic algorithm that could curate any topic.

**Built:**
- `backend/src/chronicle/providers/curated/concert_of_europe/` — real Source/Document/Passage records for three primary documents (the Congress of Vienna's General Treaty, the Troppau Protocol, and Castlereagh's State Paper of 5 May 1820), 4 real Person entities (Metternich, Alexander I, Castlereagh, Canning) and 5 real Place entities with genuine coordinates, 5 events dated within their real historical ranges, 5 claims, and 3 relationships — including one genuine `disputed` relationship (Britain's rejection of the Troppau doctrine) carrying both a supporting and a counterevidence link, the same validator rule 7 path C2's mock relationship exercised, now with real content.
- **Sourcing discipline, disclosed per-source:** the Vienna Final Act is cited to a directly hosted, independently verified public-domain full text (Wikisource); the Troppau Protocol's key clause is quoted directly because it is independently corroborated with consistent wording across multiple secondary descriptions, with the gap that no directly hosted full primary transcription was located explicitly disclosed; Castlereagh's State Paper is represented as an honest paraphrase, not a fabricated verbatim quotation. No quotation in this package is invented.
- **A real, rights-cleared period map for one scene only.** William R. Shepherd, *Historical Atlas* (1911), "Treaty Adjustments, 1814, 1815," p. 157 (PCL-digitized, public domain) — downloaded to `public/maps/concert-of-europe/`, documented in `docs/research/concert-of-europe-map-source.md` (bounds read from the plate's own printed reference grid by visual inspection, disclosed as an approximation). Deliberately **not** attached to the second scene ("The Principle of Intervention, 1820-1822"): the plate documents the 1815 settlement, not the Troppau/Laibach/Verona-era situation five to seven years later, so the honest choice is no map for that scene, not a misfit one — disclosed in `generationReport.omissions`, the same discipline `docs/research/scene-2-map-source.md` established for its own gap.
- **A second genuine, disclosed gap:** no dedicated Congress of Verona primary document was independently located and curated in this pass, so `claim-verona-spain` and its relationship are supported only indirectly, via continuity with the Troppau doctrine — stated plainly in the package's own omissions, not hidden.
- **A real bug caught and fixed during integration, not shipped:** registering the generated package in the frontend surfaced that Pydantic's `model_dump(mode="json")` serializes unset `Optional[...]` fields as `null`, while the TS/Zod contract's `.optional()` fields require the key to be *absent*, not present-as-null — a present-but-null optional field failed frontend schema validation even though it passed Python's validator. Fixed in `providers/verification.py` by adding `exclude_none=True` to the final serialization; this fix applies to the mock provider set too, closing a latent C2 gap that had gone uncaught because no C2 mock package had previously been round-tripped through the frontend's Zod schema.
- `--provider-set {mock,concert-of-europe}` added to `chronicle generate`/`resume` (default `mock`, so every existing C0-C2 test's behavior is unchanged); `cli/commands.py`'s `PROVIDER_SETS` lookup table selects the `(stage_fns, provider_set_version)` pair. No changes to `engine.py`'s stage-execution contract were needed — the curated provider set plugs into exactly the same `StageFn` shape the mock set uses.
- `fixtures/concert-of-europe.generated-investigation.json` — the frozen, checked-in output of `chronicle generate "The Concert of Europe and Revolutionary Intervention" --provider-set concert-of-europe`, confirmed byte-identical across two independent runs before freezing. `src/content/investigationFixtures.ts` registers it (`isDefault: false`) alongside the golden and empty fixtures.
- `tests/e2e/concert-of-europe-journey.spec.ts` — two new Playwright journeys: the Vienna scene (real map citation renders, narrative → evidence sync, cross-facet place selection) and the intervention scene (schematic-fallback map with its own honest "no period-accurate basemap curated for this scene yet" label, disputed-relationship evidence rendering visible classification text, not color alone).

**Verification:** Python — 59/59 pytest passed (49 existing + 10 new: 2 pipeline + 7 content-accuracy + 1 CLI). Manual: `chronicle generate ... --provider-set concert-of-europe` then `chronicle validate` on its own output — confirmed valid; reran and diffed the two runs' `output/package.json` byte-for-byte identical before freezing into `fixtures/`. Frontend — `npm run typecheck`, `npm run lint`, `npx vitest run` (77/77), `npm run build` all clean; `npm run test:e2e` — 7/7 passed, including both new Concert-of-Europe journeys alongside all pre-existing journeys unmodified.

**Explicitly not in C3:** no live search/retrieval/embeddings/model calls (still deferred past Phase C). No changes to `engine.py`'s stage contract. No third package.

**No commit, no push, no merge** on C0-C3 — per `AGENTS.md` §9, that needs separate explicit approval. This closes out the Phase C roadmap from the adjusted plan.

### Codex Handoff: Phase C — Python Deterministic-Generation Backend (C0-C3)

**Feature or phase:** Phase C (all four sub-plans: C0 contract parity, C1 workflow engine, C2 mock providers, C3 real curated package + frontend integration), per `chronicle_phase_c_adjusted_plan.md`.
**User problem:** Prove that a deterministic, resumable, file-persisted Python pipeline can produce packages conforming exactly to the existing `GeneratedInvestigation` contract — first mechanically (C0-C2, synthetic content), then for real (C3) — as the foundation the eventual AI-generation layer (Phase D+) will build on.
**Expected user outcome:** `chronicle generate <topic> [--provider-set mock|concert-of-europe]` produces a schema-valid package; the frontend renders a second, independently sourced investigation through the same generic route/components as the original hand-authored one, with no hidden coupling.
**Relevant product documents:** `chronicle_phase_c_adjusted_plan.md` (supplied by Kamal, not checked into the repo as a file), `AGENTS.md` §4 (deterministic software vs. provider-layer boundary) and §12 (historical-integrity discipline).
**Relevant architecture documents:** `docs/architecture/spatial-architecture.md` (map/period-fit requirements), `docs/research/scene-2-map-source.md` (the disclosed-approximation pattern C3's map doc follows).
**Files changed:** see the C0/C1/C2/C3 "Built" sections above; C3 specifically touches `backend/src/chronicle/providers/curated/concert_of_europe/*` (new), `backend/src/chronicle/providers/verification.py` (`exclude_none` fix), `backend/src/chronicle/cli/{main,commands}.py` (`--provider-set`), `backend/tests/providers/curated/*` (new), `backend/tests/cli/test_cli.py` (new test), `docs/research/concert-of-europe-map-source.md` (new), `public/maps/concert-of-europe/*` (new), `fixtures/concert-of-europe.generated-investigation.json` (new), `src/content/investigationFixtures.ts` (registration), `tests/e2e/concert-of-europe-journey.spec.ts` (new).
**Database changes:** none (file-based run storage only, gitignored).
**API changes:** none (no HTTP API yet — CLI only).
**Tests added:** 59 Python tests total (C0-C3 combined); 2 new Playwright journeys; frontend vitest suite grew implicitly via the new fixture (77 tests, unchanged count — fixture validity is exercised through existing repository/page tests).
**Commands run:** `pytest -v` (backend, all sub-phases); `chronicle generate`/`resume`/`inspect`/`validate` manually for both provider sets; `npm run typecheck`/`lint`/`build`; `npx vitest run`; `npm run test:e2e`.
**Known limitations:** the Concert of Europe package cites the Troppau Protocol and Castlereagh's State Paper via secondary reprints/paraphrase rather than independently verified primary-text transcriptions (disclosed in each Source's `knownLimitations`); the Verona/Spain claim is only indirectly evidenced (disclosed in `generationReport.omissions`); the intervention scene has no period map by design (disclosed, not a bug). None of these are silently hidden — they are the actual point of the disclosure discipline this package exercises.
**Risks:** the `exclude_none` fix changes serialization behavior for every future provider set (mock included) — low risk since it only removes already-optional, already-unset fields, but worth an explicit look given it touches a shared code path.
**Acceptance criteria:** package passes `validate_generated_investigation()`/the frontend's Zod schema unmodified; two independent generations of the same topic are byte-identical; the frontend renders it through the unmodified generic route; a second reviewer should be able to trace every claim in the package back to a disclosed source and confirm no fabricated quotation exists.
**Specific areas requiring independent review:** the historical accuracy of the curated content itself (dates, figures, the Troppau Protocol quotation, the paraphrases) — Codex checks software behavior per `docs/delivery/codex-review-process.md` Review Type D, not independent historical truth; a human with domain expertise should still spot-check the specific claims before this content is treated as anything beyond a prototype curation pass.

## Completed: Phase D0.1 — Map-First Workspace: Interaction Architecture Docs + ADR

Kamal supplied a major product/UX correction (`docs/product/map-first-workspace-instructions.md`): Chronicle becomes a map-first workspace — persistent bounded map canvas, a docked (desktop)/bottom-sheet (mobile) assistant panel with Ask/Explore/Evidence/Sources tabs, historical "lenses," a new `InvestigationExperiencePlan` contract, and an "Inspector" mode preserving the current article-first renderer. The source document's own §24 instruction required a 16-item audit/proposal before any editing; that audit (research via two parallel Explore agents over the current frontend and the existing product/architecture/delivery docs) was presented and approved, along with four scoping decisions, before D0.1 started.

**Decisions confirmed by Kamal:** insert this work as the new Phase D in full, shifting the former Phase D ("Real Discovery and Assessment") through former Phase K to Phase E through Phase L (not a "D0" sub-phase squeezed in); `InvestigationExperiencePlan` is optional/additive on `GeneratedInvestigation`, not required immediately; `/`'s current redirect-to-default-package behavior stays as-is until D0.5 (the Ask entry surface is a later, separate slice); sub-plans D0.1→D0.6 proceed one at a time with separate approval, mirroring C0-C3.

**Built (documentation only, no code):**
- `docs/product/map-first-workspace-instructions.md` — the source document, preserved verbatim (avoiding the earlier gap where the Phase C adjusted plan was never saved to the repo).
- `docs/decisions/ADR-002-map-first-workspace.md` — records the map-as-primary-canvas, docked-panel/Inspector split, optional experience-plan field, phase-relettering, and systems-view-split decisions plus alternatives considered; added to `docs/decisions/README.md`'s index.
- `docs/delivery/revised-development-phases.md` — Phase D is now "Map-First Investigation Experience"; the former Phase D through Phase K shifted to Phase E through Phase L, each phase's description otherwise unchanged.
- Cascading phase-letter reference fixes: `docs/architecture/system-overview.md`, `docs/architecture/frontend-architecture.md` (also documents the new `workspace/`/`inspector/` directories planned under `src/features/investigation/`), `docs/architecture/generated-investigation-contract.md`.
- `docs/delivery/risks-and-open-questions.md` (new risk: map-first redesign regressing the article-first experience it replaces as default) and `docs/delivery/definition-of-done.md` (new criterion: reduced-motion + keyboard panel control, required starting Phase D).

**Explicitly not in D0.1:** no code, no contract changes, no new tests, no UI changes — those start in D0.2/D0.3.

**No commit, no push, no merge** — same standing rule as every prior phase.

## Completed: Phase D0.2 — `InvestigationExperiencePlan` Contract + Parity

Contract-only slice, no UI changes. Adds `experiencePlan` as an **optional, additive** field on `GeneratedInvestigation`/`GeneratedInvestigation` (Python) — `blank-cheque.golden-investigation.json` and `concert-of-europe.generated-investigation.json` both still validate unmodified, confirmed by test.

**Built:**
- `src/features/investigation/model/experiencePlan.ts` — `MapScope`, `InvestigationLens` (with a **required** `textFallback` field per the accessible-equivalent requirement, map-first-workspace-instructions.md §17.1), `StorySequence`, `SystemPath`, `PerspectiveComparison`, `ContextualPromptSet`, `SelectionTarget`, `InvestigationLimitation`, `InvestigationExperiencePlan`, and `validateExperiencePlanReferences()` — a cross-reference validator taking pre-resolved id-sets and the existing `requireReference` helper as parameters (dependency injection, not a circular import back into `generatedInvestigation.ts`).
- `generatedInvestigation.ts` — `experiencePlan` added to the schema as `.optional()`; `fail`/`requireReference` exported for reuse; `ensureUniqueIds` extended to include the plan's own collections when present; `validateExperiencePlanReferences` called at the end of `validateGeneratedInvestigation()` with an `anyRecordIds` set (entities ∪ events ∪ claims ∪ relationships ∪ knowledgeStates ∪ decisions ∪ communications ∪ sources ∪ documents) for `SystemPath.nodeIds`' intentionally broad reference target.
- `backend/src/chronicle/contracts/experience_plan.py` — the exact Pydantic mirror, same field order and constraints; reuses the existing `FocusKind` enum for `SelectionTarget.kind` (already the identical 8-value set) rather than duplicating it, and the existing `Coordinates`/`HistoricalDate` shared models. New `PanelTab`/`EvidenceDepth` enums added to `enums.py`.
- `validation.py` — `_validate_experience_plan_references()` (Rule 21) plus the same `ensureUniqueIds`/optional-field-guard pattern as the TS side.
- Two new shared fixtures — `fixtures/contracts/invalid/experience-plan-{unknown-location,unknown-initial-lens}.json` (the golden blank-cheque package plus a minimal experience plan, broken one way each) — tested from both `experiencePlan.test.ts` and `test_invalid_fixtures.py`/`test_experience_plan.py`, the same shared-invalid-fixture pattern C0 established.
- `InvestigationTimelinePlan` (play/pause/speed config) is deliberately **not** part of this slice — noted in both files' docstrings — since D0.3 hasn't built the timeline UI that would consume it yet; `workspace.initialTimeRange` covers what a contract-only slice needs.

**Verification:** TypeScript — 89/89 vitest (up from 77; 26 new in `experiencePlan.test.ts` covering acceptance, the optional-field case, 6 distinct broken-reference rejections, an ID-collision rejection, and the 2 shared invalid fixtures), typecheck/lint/build all clean, `test:e2e` 7/7 unaffected (no UI touched). Python — 71/71 pytest (up from 59; 12 new in `test_experience_plan.py` mirroring the TS cases one-for-one, plus the 2 shared fixtures wired into `test_invalid_fixtures.py`).

**Explicitly not in D0.2:** no UI, no `workspace/`/`inspector/` directories yet, no `InvestigationTimelinePlan`, no live calls.

**No commit, no push, no merge** — same standing rule as every prior phase.

## Completed: Phase D0.3 — Workspace Shell

The map-first workspace and Inspector are both live, at `/investigations/:packageId/scenes/:sceneId` and `/investigations/:packageId/scenes/:sceneId/inspector` respectively. Two small, principled contract amendments landed first (`InvestigationLens.visualizationType: 'map'|'graph'`, `SystemPath.lensId`) so a lens can declare which canvas it wants and which systems path belongs to it — added in both languages, with parity tests updated, before any UI code.

**Built:**
- `src/features/investigation/workspace/` — `InvestigationWorkspace.tsx` (top-level composition: header, persistent timeline, canvas, panel), `WorkspaceHeader.tsx` (title, live focus-description, lens `<select>`, Inspector link), `lenses/lensResolution.ts` (`resolveLenses` — reads `investigation.experiencePlan.lenses`, or synthesizes a single "Overview" lens from the scene's own data for packages without a plan yet, e.g. the empty-content fixture; `sceneScopeLens` — intersects a lens's investigation-wide visible-id lists with what the *current* scene actually contains, since a lens can span scenes but the workspace renders one at a time), `lenses/useActiveLensId.ts` (URL-persisted, deep-linkable), `canvas/SystemsLensView.tsx` (the labelled-node graph replacing numbered claim IDs for `visualizationType: 'graph'` lenses — real historical-object labels resolved from whichever scene collection an id belongs to, readable edge verbs from `relationshipType`, evidence-strength as line style plus text, never color alone), `dock/{usePanelState,PanelTabs,DockedPanel,BottomSheet}.tsx` (desktop resizable/collapsible panel with a keyboard- and pointer-operable `role="separator"` divider, focus moved to/from the reopen button on collapse/reopen; mobile bottom sheet with a real, keyboard-operable cycle button, not gesture-only), `panel/{AskTab,ExploreTab,EvidenceTab,SourcesTab}.tsx` (Ask is an honest placeholder for D0.5, not a fake assistant; Explore is lens-aware; Evidence is deliberately shallow with a hand-off to Inspector, not a reskin of the existing deep `EvidencePanel`; Sources lists scene corpus coverage).
- `src/features/investigation/inspector/InspectorView.tsx` — the exact prior article-first rendering (`NarrativeView`/`TimelineView`/`MapView`/`GraphView`/`EvidencePanel` via the unchanged `InvestigationLayout`), with a "← Back to workspace" link added.
- `MapView.tsx`/`TimelineView.tsx` — extended with optional `placeIds`/`eventIds` filter props (omitted → unchanged behavior, which is what Inspector uses; supplied → workspace's lens-scoped rendering). No duplicated map/timeline rendering logic between workspace and Inspector.
- `InvestigationPage.tsx` — now a shared loader/error/pending shell with a `mode: 'workspace' | 'inspector'` prop picking which view renders once data resolves; `routes.tsx` adds the `/inspector` route segment.
- `src/content/investigationFixtures.ts` — hand-authored `InvestigationExperiencePlan`s for both blank-cheque (3 lenses: Sequence, Systems — the real disputed causal-influence relationship rendered as a labelled 2-node graph, Uncertainty) and concert-of-europe (3 lenses, directly reusing the systems path worked out in the D0 audit: Troppau→established→Naples, Castlereagh→disputes→Troppau, Verona→extends (indirectly)→Troppau; a perspective comparison; the two disclosed gaps surfaced as first-class Uncertainty-lens content). Both on-disk fixture JSON files stay untouched — the plans attach via a spread in code, preserving blank-cheque's "migrated human-curated draft" and concert-of-europe's "frozen pipeline output" provenance stories. D0.4 replaces the hand-authored concert-of-europe plan with one generated by the curated pipeline itself.
- `gate1-journey.spec.ts` and `keyboard-navigation.spec.ts`'s Map/Graph-tab and narrative-button tests now target `/inspector` explicitly, since that's where the mechanics they test still live unchanged. `concert-of-europe-journey.spec.ts` was rewritten to exercise the *workspace* instead (proving the new shell is genuinely generic across two independently-sourced packages, which was always that spec's point). New `workspace-journey.spec.ts` covers what's actually new: keyboard panel resize/collapse/reopen with correct focus management, lens-selection URL persistence across reload, the mobile bottom sheet's cycle behavior, and a full-page axe scan.

**A real accessibility bug caught and fixed, not shipped:** the initial build had `ExploreTab`/`SystemsLensView` headings at `<h3>` with no intervening `<h2>` anywhere in the workspace (WorkspaceHeader only has `<h1>`) — a heading-order violation axe caught immediately via the new `workspace-journey.spec.ts` a11y test. Fixed by using `<h2>` there instead.

**Explicitly not in D0.3:** the Ask tab is a disabled placeholder (D0.5 builds the real conversational surface); `InvestigationTimelinePlan` (play/pause/speed) still isn't consumed — the timeline shows lens-filtered events but has no transport controls yet; no live pipeline-generated experience plan (D0.4); no manual `agent-browser` walkthrough was performed this pass — the 11 real-Chromium Playwright journeys (including two full a11y scans and a mobile-viewport journey) are the verification for this slice instead.

**Verification:** Python — 71/71 pytest (unchanged from D0.2, no backend touched). TypeScript — 91/91 vitest (up from 89: `InvestigationPage.test.tsx` and `InvestigationSync.test.tsx` adapted, not silently passed through — the latter now explicitly exercises Inspector, since that's where five-facet sync mechanics live). Playwright — 11/11 (up from 7: 2 rewritten Concert-of-Europe workspace journeys, 2 Inspector-redirected keyboard-navigation tests, 4 new workspace-journey tests, 3 unchanged). typecheck/lint/build all clean.

**No commit, no push, no merge** — same standing rule as every prior phase.

## Completed: Phase D0.4 — Concert of Europe Experience Plan Through the Curated Pipeline

Replaces D0.3's hand-authored Concert of Europe `InvestigationExperiencePlan` (which lived only in `src/content/investigationFixtures.ts`) with one the curated Python pipeline actually generates — the same proof C3 established for the base package: no React branching, just `investigation.experiencePlan` read as data.

**Built:**
- `backend/src/chronicle/providers/curated/concert_of_europe/experience_plan.py` — a new `attach_experience_plan(data)` stage function, deterministic, over the same real facts `composition.py`/`relationships.py`/`timeline.py` already curated: three lenses (Sequence, Systems, Uncertainty), one story sequence (Troppau→Laibach→Naples), one system path (`path-intervention-doctrine`, reusing the real `rel-troppau-supports-naples`/`rel-castlereagh-disputes-troppau`/`rel-verona-extends-troppau` relationships), one perspective comparison (Metternich vs. Castlereagh). Field-for-field mirror of what D0.3 hand-authored — no new historical research, only new *presentation* data over facts already cited.
- `backend/src/chronicle/providers/curated/concert_of_europe/registry.py` — `CONCERT_OF_EUROPE_STAGE_FNS[StageName.INVESTIGATION_COMPOSED]` now composes `compose_investigation` with the new `attach_experience_plan`; `CONCERT_OF_EUROPE_PROVIDER_SET_VERSION` bumped to `"d0.4-concert-of-europe-v2"` (a real content change to the composed output, not just a version-string formality — this is exactly the reuse-vs-rerun hash-invalidation path C2 built).
- `backend/tests/providers/curated/test_concert_of_europe_experience_plan.py` — new: the generated package has all 3 expected lens ids; the Systems lens's `visibleRelationships` matches the 3 real curated relationship ids and is `visualizationType: 'graph'`; the Uncertainty lens's limitations mention both disclosed gaps (Verona, map); the system path and perspective comparison reference only ids that actually exist in the generated `claims`/`relationships`/`entities`; two independent generations produce byte-identical `experiencePlan` content.
- `backend/tests/providers/curated/test_concert_of_europe_pipeline.py`, `test_concert_of_europe_content.py`, `backend/tests/cli/test_cli.py` — updated to reference `CONCERT_OF_EUROPE_PROVIDER_SET_VERSION` instead of the old hardcoded `"c3-concert-of-europe-v1"` string (one CLI test asserted the literal version string and needed a matching update).
- `fixtures/concert-of-europe.generated-investigation.json` — regenerated via `chronicle generate "The Concert of Europe and Revolutionary Intervention" --provider-set concert-of-europe`, confirmed byte-identical across two independent runs, validated via `chronicle validate`, and re-frozen with `experiencePlan` now baked directly into the file (previously absent — D0.3 attached it only in frontend code).
- `src/content/investigationFixtures.ts` — the hand-authored `CONCERT_OF_EUROPE_EXPERIENCE_PLAN` TS constant and its manual `{...json, experiencePlan: ...}` attachment are deleted; `concertOfEuropeInvestigation` now validates the fixture JSON directly, since the plan is already part of it. `BLANK_CHEQUE_EXPERIENCE_PLAN` is unchanged and stays hand-authored — that package has no generation pipeline behind it.

**Explicitly not in D0.4:** no changes to the lens/systemPath *content* itself (same three lenses, same relationships, same disclosed gaps as D0.3's hand-authored version) — this slice is about *where* the plan comes from, not what it says. No live search/retrieval/embeddings/model calls. No changes to `engine.py`'s stage contract.

**Verification:** Python — 76/76 pytest passed (up from 71: 5 new in `test_concert_of_europe_experience_plan.py`), including a determinism check on the generated `experiencePlan` specifically. Frontend — `npm run typecheck`, `npm run lint`, `npx vitest run` (91/91, unchanged — the fixture's validity is exercised through existing repository/page tests, no new test needed since the plan's *shape* didn't change), `npm run build` all clean. `npm run test:e2e` — 11/11 passed unchanged (the workspace-journey and concert-of-europe-journey specs assert against lens ids/content, which are identical to D0.3's hand-authored version by design).

**No commit, no push, no merge** — same standing rule as every prior phase.

## Completed: Phase D0.5 — Initial Interaction Prototype (Ask Entry Surface)

`/` is now the Ask entry surface (`docs/product/map-first-workspace-instructions.md` §4-6) instead of an unconditional redirect into the golden fixture. Before implementing, two product decisions needed resolving that the source document doesn't address — it assumes a live generation backend exists behind the Ask surface, and Chronicle doesn't have one yet (no HTTP API; the Python backend is CLI-only; live model/search calls stay deferred past Phase C by design). Kamal's explicit choice: (1) submitting a question keyword-matches against the investigations that actually exist, landing in the real matched package, with an honest "not available in this prototype" message on no match rather than a silent wrong guess; (2) the mock generation-progress checklist uses the real 8 `StageName`s from the Python pipeline, animated over a few seconds, not invented copy.

**Built, in a new `src/features/investigation/ask/` directory (parallel to `workspace/`/`inspector/`):**
- `topicMatch.ts` — `matchInvestigationToQuestion()`, a pure function scoring shared significant words between a typed question and each investigation's own real `experiencePlan.opening.question`/`scopeSummary` + `presentation.title`. No hardcoded keyword list disconnected from real content — it reads the same validated fixture data every other workspace component reads. Returns `null` below a small match threshold, and for investigations without an `experiencePlan` at all (e.g. the empty-content fixture).
- `AskEntryPage.tsx` — the page mounted at `/`, a small state machine: ask → scope-review → generating → navigate into the real workspace route, or ask → no-match (stays put, offers the two real investigations' own opening questions as one-click "suggested starting points" that are guaranteed to match, rather than the source document's generic, unverified capability bullets).
- `ScopeReviewCard.tsx` — shows the matched investigation's real `opening.question`/`scopeSummary`/`leadAnswer` under a "Proposed scope" heading, with exactly two real actions (Generate this investigation / Ask something else) — not the source document's fuller action set (Edit scope, Expand timeframe, ...), since those would be non-functional buttons with no real destination given there's no live pipeline behind this prototype.
- `GenerationProgress.tsx` / `stageNames.ts` — a checklist ticking through the real 8 `StageName`s hand-mirrored from `backend/src/chronicle/workflow/stages.py` (`SCOPE_PROPOSED` → `VERIFIED`), ~350ms/stage, then calls back to navigate into the matched investigation's default scene.
- `useReducedMotion.ts` — a small `matchMedia('(prefers-reduced-motion: reduce)')` hook, new to this codebase (D0.3's panel transitions were CSS-only, covered by the existing global rule in `src/index.css`; this is the first JS-driven timed animation, so it needed its own check) — collapses the progress checklist to near-instant under reduced motion, per D0.1's added definition-of-done criterion.
- `src/app/routes.tsx` — `/`'s route element is now `<AskEntryPage />`; `getDefaultInvestigationRoute()` stays in `investigationRepository.ts`, used only by its own test now.

**A real accessibility bug caught and fixed, not shipped:** the initial `AskEntryPage` wrapped its content in a plain `<div>`; axe's landmark rule flagged "page content not contained by landmarks" on both the new ask-entry Playwright spec and the pre-existing `investigation.spec.ts` (which also hits `/`). Fixed by wrapping in `<main>`, the same pattern every other top-level page in this codebase already uses (`InvestigationWorkspace.tsx`, `InvestigationLayout.tsx`, `InvestigationPage.tsx`'s loading/error states).

**Existing test adapted, not silently passed through:** `keyboard-navigation.spec.ts`'s first test (`page.goto('/')`, expecting an immediately-visible blank-cheque timeline button) now hits the Ask surface instead of the workspace — redirected to the direct workspace URL, the same fix already applied to that file's other two tests in D0.3.

**Explicitly not in D0.5:** no live search/retrieval/embeddings/model calls (still deferred past Phase C). No changes to the persistent, in-workspace Ask tab (`AskTab.tsx`'s docked-panel placeholder) — that's `map-first-workspace-instructions.md` §15's separate, larger contextual-assistant feature, confirmed out of scope for this slice against the original D0 audit's own D0.5 description. No conversation-history persistence into that tab (there's nothing there yet to persist into). No changes to the Python backend — this slice is presentation-only, no new contract.

**Verification:** Python — 76/76 pytest (unchanged, no backend touched). TypeScript — 101/101 vitest (up from 91: 5 new in `topicMatch.test.ts`, 5 new in `AskEntryPage.test.tsx`), typecheck/lint/build all clean. Playwright — 14/14 (up from 11: 3 new in `ask-entry-journey.spec.ts` — matched-question journey through to a real workspace landing, honest no-match message, full-page axe scan).

**No commit, no push, no merge** — same standing rule as every prior phase. This closes out the six-sub-plan Phase D0 sequence (D0.1-D0.5) from the original audit; D0.6 (the human-comprehension usability-gate test plan document) is the one remaining item.

## Completed: Phase D0.6 — Usability Gate Test Plan

Documentation only, no code — mirrors exactly how Phase 1's Gate 1 was handled: an actual 5-participant human comprehension test is outside what Claude can run (no ability to recruit people, schedule sessions, or observe genuine human reactions), so this slice automates what *can* be automated (already done across `workspace-journey.spec.ts`, `ask-entry-journey.spec.ts`, `concert-of-europe-journey.spec.ts` — mechanics, keyboard operability, no detectable a11y violations) and writes the human-comprehension test plan as a document for someone to actually run.

**Built:**
- `docs/delivery/phase-d0-validation-plan.md` — the plan, structured like `docs/delivery/phase-1-validation-plan.md`: the decision being tested (does the map-first workspace help a reader investigate at least as well as Inspector, without confusion or overstated certainty), an 8-task list covering the Ask flow, lens switching, the Systems lens's relationship/evidence-strength encoding, panel resize/collapse, finding a disclosed limitation, and the Inspector hand-off, plus a Gate D0 success threshold.
- A genuine addition beyond the Phase 1 precedent, not a mechanical copy: **Task 8 is an explicit disclosure check** — "When you asked your question, what did you think the system did behind the scenes?" — because D0.5's Ask entry surface keyword-matches against 2 existing packages rather than performing real generation (`src/features/investigation/ask/topicMatch.ts`). A participant who comes away believing the system did bespoke live research for their specific question is a blocking defect under this plan's threshold, not a minor note — the same honesty discipline this project has applied to historical content (C2's disclosed mocks, D0.3's disclosed placeholder Ask tab) extended to the interaction pattern itself.
- `docs/delivery/validation/phase-d0-gate-template.md` — the blank fill-in report, mirroring `phase-1-gate-1-template.md`'s structure (participant profiles, per-task results table, observations, severity-ranked issues, threshold checklist, proceed/revise/stop decision), plus a dedicated Disclosure Check table for Task 8.

**Explicitly not in D0.6:** no actual human study was run (that requires a human to recruit and facilitate participants); no code changes.

**No commit, no push, no merge** — same standing rule as every prior phase. **This closes out the full Phase D0 sequence (D0.1–D0.6)** from the original audit approved at the start of this phase.

Phase D0 (map-first workspace) is fully sequenced and built, pending the actual human usability study (`docs/delivery/phase-d0-validation-plan.md`) before being treated as validated.

## Completed: Phase E0 — Reconcile Repository State and Reorient Toward the AI Core

Kamal supplied a second, larger product correction: the domain-specialized LLM/multi-agent system is Chronicle's actual product core, not the historical content around it — the historical domain (existing curated investigations, reframed as benchmark corpora) is the AI system's proving ground. Source instructions preserved verbatim at `docs/ai-core-instructions/00_START_HERE.md` through `06_CLAUDE_CODE_EXECUTION_INSTRUCTIONS.md` (encoding-corruption from the paste fixed — em dashes/arrows/tree-drawing characters reconstructed, no wording changed, noted in each file's header comment). Document `06`'s own instruction required a 20-item audit and proposed plan before any implementation — delivered, approved, and scoped per `CLAUDE.md` rule 6 to only the smallest first slice (E0, pure reorientation), not an attempt to build E1-E9 at once, mirroring exactly how the D0 map-first correction was sequenced.

**Two decisions resolved before implementation, since they either conflicted with a standing written constraint or matched the weight of a prior explicitly-approved pivot:**
- **Model provider: local open-weight via Ollama**, not a paid hosted API — stays inside `AGENTS.md` §5's existing "no paid infrastructure" constraint exactly as written, no cost exception requested. Accepted tradeoff, recorded in the ADR: a smaller local model is measurably weaker at reliable structured output/tool use than a frontier paid API, which puts real weight on Phase E1's retry-on-malformed-output requirement and on Phase E7's evaluation harness actually being able to show the four-agent design earns its complexity. Ollama is not installed on this machine yet — a manual setup step for Kamal, not a code blocker for E1 (real-provider integration tests are designed to skip gracefully when Ollama isn't reachable).
- **Roadmap: full replacement**, not an insertion. `docs/delivery/revised-development-phases.md`'s Phase E-L section is replaced wholesale with the new document set's Phase E-K sequence (E=AI Core, F=Autonomous Source Discovery, G=Document Acquisition/RAG, H=Historical Model Generation, I=Task Model Experiment, J=AI Geographic/Experience Composer, K=Full Autonomous Investigation). The former Phase E/F content isn't lost — it's absorbed into new F/G, resequenced behind the AI core.

**Built:**
- **Checkpoint commit** (`b0380e2`, pushed to `origin/phase-c-python-foundation`): all of Phase C0-C3 + D0.1-D0.6 — 78 files, previously uncommitted despite `plans/current-phase.md` already describing it as done. Keeps the Phase E diff reviewable on its own rather than tangled with two phases' worth of prior work.
- `docs/ai-core-instructions/` — the six source documents, verbatim (content), with the paste's mojibake corruption mechanically fixed.
- `docs/decisions/ADR-003-llm-agent-system-is-product-core.md` — records the product-framing correction, the four-agent architecture (Investigation Planner/Evidence Analyst/Historical Critic/Investigation Guide), the Ollama decision and why it satisfies `AGENTS.md` §5 rather than needing an exception, the roadmap replacement, and Phase E's explicit exclusions (no unrestricted web research, no production vector DB, no fine-tuning yet) as consequences. Added to `docs/decisions/README.md`'s index.
- `docs/delivery/revised-development-phases.md` — Phase E-L replaced with the new E-K sequence.
- Additive "Phase E update"/"Phase E note" sections (not full rewrites, matching D0.1's discipline) on `docs/architecture/ai-agent-architecture.md` (ties its existing bounded-pipeline description to the four now-named agents), `docs/product/investigation-assistant.md` and `docs/architecture/investigation-assistant.md` (same reconciliation, plus pointing "Phase 4" references at the current Phase E1-E6 sequencing), and `docs/architecture/provenance-and-review.md` (flags its Postgres/pgvector/"Studio" description as aspirational/not-yet-built, so a future reader doesn't assume it exists — only file-based `RunStore` persistence exists today).

**A real audit finding, not assumed:** `engine.py`'s `run_pipeline()` (the existing workflow engine) is shaped for one fixed linear 8-`StageName` sequence producing a new `GeneratedInvestigation` *package* — it is not the right shape for an agent run (a bounded, dynamic tool-calling loop that *answers a question against an already-generated package*). Recorded in the ADR: Phase E's agent orchestration reuses the *pattern* (file-based `RunRecord`/`StageRecord`-style persistence, hash-based dedup idioms) inside a new, sibling `ai/orchestration/` module, not the literal `StageName` enum or `run_pipeline()` call path — flagged now so it isn't discovered as a surprise mid-E1.

**Explicitly not in E0:** no `ai/`, `api/`, or `corpus/` code (E1-E5). No frontend changes (E6) — `AskTab.tsx` stays exactly as its D0.3 disclosed placeholder. No evaluation harness (E7). No benchmark corpora beyond the existing two (E8). No live model calls of any kind — Ollama isn't even installed yet.

**Frontend freeze in effect starting now** (per the ADR): no new static investigations, no new lenses without an AI consumer, no more hand-authored provider sets, no more deterministic-"generation" UI simulations, until the E1-E6 slices land.

The checkpoint commit (`b0380e2`) is pushed. This E0 documentation work is committed and pushed separately, per Kamal's explicit instruction — see the commit/push confirmations below.

## Completed: Phase E1 — Model Provider Foundation

Branch `phase-e-ai-core`, not yet committed (per Kamal's explicit instruction, awaiting separate approval before committing/pushing E1). A provider-agnostic `ModelProvider` protocol, a deterministic test provider, and a real `OllamaModelProvider` — the foundation the four agents (E3-E4) will consume, with no agent built yet.

**Hardware audit performed before any model was chosen** (recorded in full in `docs/ai/model-provider-decisions.md`): Windows 11, AMD Ryzen 5 6600H (6 cores/12 threads), **13.69 GB total RAM**, integrated AMD GPU with no realistic Ollama acceleration path on Windows, Ollama not installed. This is a real constraint, not a formality — it cut the practical model ceiling to ~7-8B parameters at Q4 quantization, CPU-only, before any capability comparison mattered.

**Model comparison presented and approved** (Qwen2.5:7b-instruct vs. Qwen2.5:3b-instruct vs. Llama3.1:8b-instruct, full table in `docs/ai/model-provider-decisions.md`): **Qwen2.5:7b-instruct** is the configured default (`OllamaModelProvider`'s `DEFAULT_MODEL`), reused sequentially across all four future agent roles rather than four separately-loaded models. **Qwen2.5:3b-instruct** is documented as the fallback for the Guide role or fast local iteration, switchable via config with no code change. **No model has been downloaded and Ollama has not been installed** — that remains a manual step for Kamal, reported here rather than done silently, exactly as instructed.

**Built, in a new `backend/src/chronicle/ai/` package:**
- `ai/models/errors.py` — an explicit failure taxonomy (`ProviderUnavailableError`, `ModelUnavailableError`, `ProviderTimeoutError`, `RateLimitError`, `MalformedOutputError`, `SchemaValidationError`, `RetryExhaustedError`, `InvalidConfigurationError`, `UnsupportedCapabilityError`, `RequestCancelledError`) — every failure path in both providers raises one of these, never a bare `Exception`.
- `ai/models/metadata.py` — `ModelCallRecord` (deliberately shaped like `workflow/state.py`'s existing `StageRecord`: same `attemptCount`/`startedAt`/`completedAt`/`errorType` fields, a proven pattern reused not reinvented), `ProviderMetadata` (provider identity/capability only — no model fields), `ProviderHealth`, `TokenUsage` (omitted, never fabricated, when a provider's response doesn't report it), and — added during closeout, see "Closeout corrections" below — `ProviderCost`/`CostBasis` for provider-billed cost recording.
- `ai/models/protocol.py` — `ModelProvider`, a `@runtime_checkable` structural `typing.Protocol` (`generate_structured`, `generate_text_from_verified_records`, `health_check`, `provider_metadata`), synchronous by design (matching 100% of the existing codebase; async deferred to E5's FastAPI layer, which can run a sync call in a threadpool).
- `ai/models/deterministic.py` — `DeterministicModelProvider`: no network calls, a FIFO-scripted queue of responses/errors/malformed payloads per test, used by every future agent's tests so none of them depend on live inference (`AGENTS.md` §6).
- `ai/models/ollama.py` — `OllamaModelProvider`: talks to a local Ollama server over `httpx`, uses Ollama's JSON-schema-constrained structured-output mode (`format: response_model.model_json_schema()`), validates through Pydantic on top as a second gate, retries once with the validation error appended as concise feedback on malformed/schema-invalid output before raising `RetryExhaustedError` honestly. Base URL from `CHRONICLE_OLLAMA_BASE_URL` (no API key — Ollama needs none).
- `ai/orchestration/policies.py` — `MAX_STRUCTURED_OUTPUT_ATTEMPTS = 2`, `RETRYABLE_ERROR_TYPES`, shared by both providers so retry behavior is identical and independently testable rather than each provider inventing its own.
- `ai/orchestration/statuses.py` / `run_models.py` — `AgentRunStatus`, `AgentRunRecord` (a validated record *shape*, unit-tested for construction/rejection since closeout — `backend/tests/ai/orchestration/test_run_models.py` — but not wired to a runner and never durably stored; Planner/Analyst/Critic/Guide in E3/E4 are what will actually drive and persist it). **Deliberately not built on `workflow/engine.py`**: that engine's fixed linear `StageName` pipeline produces a new package; an agent run answers a question against a package that already exists via a dynamic tool-calling loop — a genuinely different shape, reusing the *pattern* (file-based record persistence) as a sibling module, not the code path. A source-level guard test asserts the scaffold never references `workflow.engine`/`run_pipeline`/`StageName`.
- `ai/contracts/structured_generation.py` — `StructuredGenerationResult[T]`/`TextGenerationResult`, plain generic dataclasses (not Pydantic — they're in-process return values, never serialized directly).

**A real design inconsistency caught and fixed during testing, not shipped:** the first draft bucketed HTTP 5xx responses under the same `ProviderUnavailableError` as a flat connection refusal, but one test implicitly expected 5xx to be retried while the connection-refused test (correctly) expected it not to be. Resolved by treating both as non-retryable — a local Ollama daemon returning 500 usually means something is actually broken (crashed model process, OOM, bad request), not a transient blip a same-millisecond retry fixes — and recorded as the first entry in `docs/ai/learning-log.md`, including the lesson that retry-eligibility needs to be a named, tested set per error type, not inferred ad hoc per call site.

**Documentation:** `docs/ai/model-provider-decisions.md` (the Ollama-first/provider-agnostic/deterministic-testable rationale, full hardware audit, model comparison, known limitations disclosed up front — structured-output risk, latency, ~8K practical context window, no GPU acceleration — the path to a Phase I fine-tuned Chronicle task model, and, since closeout, a "What is not yet durable" section and a cost-recording section), `docs/ai/agent-architecture.md` (restates the four-agent contracts as what this provider layer serves, explicit that no agent exists yet), `docs/ai/learning-log.md` (opened, first entry recorded), `docs/delivery/phase-e-ai-core-plan.md` (E0-E9 sub-plan tracker; `phase-e-validation-plan.md` deliberately deferred to E7, when there's an actual evaluation methodology to write a validation plan around, not before).

**Explicitly not in E1:** no Investigation Planner/Evidence Analyst/Historical Critic/Investigation Guide (E3-E4). No corpus retrieval tools (E2). No FastAPI endpoints (E5). No frontend assistant integration (E6). No live web search, source downloading, embeddings, or fine-tuning. No model downloaded, no Ollama installed. **No real model call of any kind has been made** — every test exercises `DeterministicModelProvider` or `OllamaModelProvider` against `httpx.MockTransport`, never a live Ollama daemon.

**Status, distinguished precisely (added during closeout, since the first write-up of this section blurred these categories):**
- **Implemented and tested now:** the `ModelProvider` protocol; `DeterministicModelProvider`; `OllamaModelProvider`'s request construction, structured-output validation, retry-with-feedback, and error mapping (all against `httpx.MockTransport`); the error taxonomy; the bounded retry policy; `ModelCallRecord`/`ProviderMetadata`/`ProviderHealth`/`TokenUsage`/`ProviderCost` as validated Pydantic contracts; provider-billed cost recording (`$0`, `NO_PROVIDER_CHARGE`, for both current providers); `AgentRunRecord`/`AgentRunStatus`'s own construction/validation/rejection behavior.
- **Contract/scaffold only, not yet exercised by anything real:** `AgentRunRecord.status`/`modelCalls`/`abstentionReason`/`errorMessage` as fields a real run will populate; `ModelCallRecord.status=FAILED`/`errorType`/`errorMessage` as fields a real failure will populate; `ProviderCost.basis=PROVIDER_BILLED` as a value only a future hosted provider would ever produce.
- **Deferred until the dynamic agent runner (E3/E4) exists:** anything constructing a real `AgentRunRecord`; anything catching a provider's raised exception and turning it into a `FAILED` `ModelCallRecord`; any durable storage of either record type (no file/database write exists yet for this package, unlike `workflow/storage/run_store.py`'s existing package-generation persistence).
- **Deferred until Ollama is installed and a model is downloaded (§13 of the prior session's handoff, `docs/delivery/claude-chat-handoff-phase-e1.md`):** any real Qwen2.5 output, at all; whether `OllamaModelProvider`'s retry-with-feedback mechanism actually helps a real model self-correct (proven only as a mechanism against scripted mocks so far); real latency numbers; `modelVersion`/digest population on `ModelCallRecord` (stays `None` until then — never invented).

**Verification:** Python — 125/125 pytest (76 pre-existing + 49 in `backend/tests/ai/`, up from 33 after closeout added cost-field assertions and the new `tests/ai/orchestration/` suite), zero live network requests (every `OllamaModelProvider` test uses `httpx.MockTransport`, confirmed by grep). Frontend — typecheck/lint clean; E1 touches no frontend file, so vitest/build/e2e are unaffected (101/101 vitest, 14/14 Playwright, clean build, all re-verified at closeout). Existing package-generation pipeline (`engine.py`, all Phase C providers) untouched and still passing.

**Closeout corrections (this session, before commit):** the initial E1 write-up and `docs/ai/model-provider-decisions.md` contained two inaccuracies caught during independent re-verification, corrected here rather than silently: (1) `docs/ai/model-provider-decisions.md` claimed `ProviderMetadata` carries `modelName`/`modelVersion` fields — it does not; those live on `ModelCallRecord` instead, now stated correctly with a "What is not yet durable" section spelling out that failure records/agent runs are contract-only, never constructed by real code yet. (2) No provider-billed cost recording existed despite `docs/ai-core-instructions/05_PHASE_E_AI_CORE_IMPLEMENTATION_PLAN.md`'s E1 scope naming "token, latency, and cost recording" — added as `ModelCallRecord.cost: ProviderCost {amountUsd, basis}`, populated as `$0`/`NO_PROVIDER_CHARGE` by both current providers, with tests. The previously-untested `AgentRunRecord`/`AgentRunStatus` scaffold also gained its own test file (`backend/tests/ai/orchestration/test_run_models.py`, 15 tests) at Kamal's explicit request, without building any runner.

**Committed and pushed** (this session, after Kamal's explicit separate approval for both actions): commit `b2626a9` on `phase-e-ai-core`, "feat(ai): add local model provider foundation" — 28 files changed, 2016 insertions(+), 5 deletions(-). `phase-e-ai-core` was previously local-only (never pushed, no upstream); `git push -u origin phase-e-ai-core` created the remote branch and set the tracking upstream, confirmed by `git branch -vv` showing `phase-e-ai-core b2626a9 [origin/phase-e-ai-core]`. Working tree clean immediately after. A Codex read-only review handoff is prepared on request, covering: genuine AI-capability addition vs. scaffolding theater, deterministic/agent separation, no topic-specific logic, no chain-of-thought persistence, honest failure/abstention paths.

## Completed: Phase E1 closeout — real-model smoke test

Per Kamal's explicit, separate approval (distinct from the E1 commit/push approval): installed Ollama, downloaded the one approved model, and ran exactly the minimal smoke test defined in the prior session's handoff (`docs/delivery/claude-chat-handoff-phase-e1.md` §13) — no more. Full result: `docs/delivery/phase-e1-real-model-smoke-test-report.md`; lesson-oriented version: `docs/ai/learning-log.md`'s matching dated entry.

**Installed:** Ollama 0.32.5 via `winget install --id Ollama.Ollama --scope user` (official upstream release, hash-verified by winget). User-level install, bound to `127.0.0.1:11434` only, no firewall rules added or changed, registered itself to launch at login (`Ollama.lnk` in the Startup folder — disclosed, not disabled, since no instruction required disabling it).

**Downloaded:** only `qwen2.5:7b-instruct` (digest `845dbda0ea48ed749caafd9e6037047aa19acfcfd82e704d7ca97d631a0b697e`, 4,683,087,332 bytes, Q4_K_M, 7.6B params, 32768-token architectural context per `ollama show` — matching this project's prior prediction on quantization, and correcting the docs' previously-unconfirmed context-window number, see `docs/ai/model-provider-decisions.md`'s updated "Known limitations"). No 3B fallback, no other models.

**Smoke test result** (`backend/tests/ai/integration/test_ollama_smoke.py`, new — marked `local_ollama_integration`, excluded from the default `pytest -q` run via `backend/pyproject.toml`'s `addopts`, confirmed by re-running the full suite before and after: 125 passed/1 deselected both times): **passed on the first attempt** — `qwen2.5:7b-instruct` produced schema-valid JSON for a trivial 3-field schema without needing the bounded retry-with-feedback path at all. Latency ~21.0 s for a 70-prompt/18-completion-token exchange. Cost recorded correctly as `$0`/`NO_PROVIDER_CHARGE` — the first real call to exercise that field outside a test double. No errors, no instability observed (no `Resource-Exhaustion-Detector` events across the whole install+pull+test window, free RAM stable before/after); a live in-call RAM sampling attempt failed for tooling reasons (disclosed honestly in the report rather than papered over) so peak-during-inference memory specifically was not captured.

**What this does and does not prove**, restated because it's easy to overstate a first success: proves the real end-to-end wiring (`OllamaModelProvider` → real Ollama → real JSON → Pydantic validation → real `ModelCallRecord`) works. Proves nothing about complex nested agent contracts, historical reasoning, tool calling, citation grounding, or multi-agent sequencing (four ~20s+ sequential calls per question) — all still untested, all E3+ work. Phase E2 has not started.

**No code defects found** — no provider corrections were needed. The only new code is the smoke test itself, its pytest marker/exclusion config, and the two documentation files above.

**Verification after the smoke test:** Python — 126/126 pytest with the marker included (125 + 1), 125 passed/1 deselected by default (confirmed the integration test never runs in ordinary CI and never requires Ollama). Frontend unaffected (no frontend file touched).

**Committed and pushed** — see this subsection's closing confirmation below for the actual commit hash.

## Next Vertical Slice — Phase E2

Corpus service and agent tools: deterministic, typed tools (`search_passages`, `get_source_metadata`, `get_claim_evidence`, `get_relationship_evidence`, `get_timeline_context`, `get_actor_knowledge_state`, `compare_sources`, `find_counterevidence`, `trace_reviewed_relationships`, `get_map_context`) over the fields already embedded in an existing `GeneratedInvestigation` package (no database — the corpus is the already-generated package itself, per the E0 audit finding on `docs/architecture/provenance-and-review.md`). Tool registry, schemas, authorization/corpus-boundary checks, result-size limits, deterministic tests across both existing packages (blank-cheque, concert-of-europe) — cross-corpus parameterization from day one, not retrofitted later for E8.

Do not add live search, downloads, embeddings, database infrastructure, or a paid model provider without separate explicit approval. Do not commit, push, or merge without separate explicit approval.
